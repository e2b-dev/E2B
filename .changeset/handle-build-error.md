---
"@e2b/cli": patch
"@e2b/python-sdk": patch
"e2b": patch
---

Add a `handleBuildError` / `handle_build_error` helper and use it in the build scripts generated by `e2b template init` and `e2b template migrate`. A missing or invalid API key (and other known errors) now print a single actionable message instead of a full internal stack trace; unexpected errors keep their stack.
