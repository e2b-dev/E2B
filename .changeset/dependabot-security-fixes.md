---
'@e2b/python-sdk': patch
'@e2b/cli': patch
'e2b': patch
---

resolve Dependabot security alerts: bump vulnerable transitive npm deps via pnpm overrides (postcss, vite, lodash, brace-expansion, picomatch, yaml, @tootallnate/once, smol-toml, flatted, minimatch) and python dev deps in poetry.lock (pytest, pytest-asyncio, python-dotenv, pygments, requests, black)
