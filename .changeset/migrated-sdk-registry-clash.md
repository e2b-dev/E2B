---
'@e2b/code-interpreter-python': patch
'@e2b/code-interpreter': patch
'@e2b/desktop': patch
---

Bump past the versions already published from the pre-migration `e2b-dev/code-interpreter` and `e2b-dev/desktop` repositories, whose contents differ from the monorepo's, so the migrated changes get published instead of being skipped as already released.
