---
'@e2b/code-interpreter': patch
---

Bump past 2.7.2, which was already published to npm from the pre-migration `e2b-dev/code-interpreter` repository with different contents, so the migrated changes get published instead of being skipped as already released.
