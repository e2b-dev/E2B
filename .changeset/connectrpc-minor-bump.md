---
'e2b': patch
---

Bump `@connectrpc/connect` and `@connectrpc/connect-web` to 2.2.0 (fixes `Http2SessionManager.verify()` hanging when the connection closes mid-verification and stops delivering stream messages after cancellation).
