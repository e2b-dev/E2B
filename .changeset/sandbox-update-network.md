---
'e2b': minor
'@e2b/python-sdk': minor
---

Add `Sandbox.updateNetwork` / `update_network` to replace a running sandbox's egress configuration (allow / deny lists, per-host rules, internet access toggle).
