output   = "models"
wipe     = true
no-tests = true
add-enum-types = true

[psql]
    whitelist = ["auth.users", "access_tokens", "envs", "team_api_keys", "teams", "users_teams"]

[auto-columns]
    created = "created_at"
    updated = "updated_at"
