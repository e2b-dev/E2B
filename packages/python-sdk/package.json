{
    "name": "@e2b/python-sdk",
    "private": true,
    "version": "0.1.10",
    "scripts": {
        "prerelease": "poetry version $(npm pkg get version --workspaces=false | tr -d \\\")",
        "build":  "poetry build && poetry config pypi-token.pypi ${{ PYPI_TOKEN }} && poetry publish"
    }
}
