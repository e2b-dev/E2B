# Install custom packages

There are two ways to install custom packages in the E2B Sandbox.

1. Create a custom sandbox with [(TODO: link)sandbox template](/todo).
2. Install a package inside the sandbox during the runtime with a package manager of your choice.

---

## 1. Create custom sandbox

Use this option if you know beforehand what packages you need in the sandbox.



## 2. Install packages during the runtime