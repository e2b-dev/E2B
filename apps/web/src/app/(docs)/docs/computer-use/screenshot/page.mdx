# Take screenshot

You can take a screenshot of the virtual desktop in the sandbox with the `screenshot` method.

<CodeGroup isRunnable={false}>
```js
TODO
```

```python
from e2b_desktop import Sandbox
desktop = Sandbox()

# Take a screenshot and save it as "screenshot.png" locally
desktop.screenshot("screenshot.png")
```
</CodeGroup>