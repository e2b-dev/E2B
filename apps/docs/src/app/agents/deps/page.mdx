export const metadata = {
  title: 'E2B Documentation - Cloud for AI Agents Apps',
  description: 'AI Agents - Install Dependencies',
}

# Install Dependencies

You can install dependencies, libraries, and packages right inside the AI Playgrounds using the [`process()`](/playgrounds/process) method.

This is especially useful when you want to run code generated by LLM that requires additional dependencies.

## Install Python packages

## Install NPM packages
