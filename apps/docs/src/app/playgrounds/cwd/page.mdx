export const metadata = {
  title: 'E2B Documentation - Cloud for AI Agents Apps',
  description: 'AI Playgrounds Working Directory',
}

export const sections = [
  { title: 'Filesystem', id: 'filesystem' },
  { title: 'Process', id: 'process' },
]

# Current Working Directory

### Current working directory for whole session

You can set working directory either for session. If not set, it will default to home directory - `/home/user`.

<CodeGroupAutoload path="cwd/session" />

### Current Working Directory Filesystem

All filesystem operations are relative to current working directory.

<CodeGroupAutoload path="cwd/filesystem"/>

### Process

If you set working directory for session, all processes will inherit it. You can override it for each process.

<CodeGroupAutoload path="cwd/process"/>
